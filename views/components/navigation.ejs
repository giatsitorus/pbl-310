<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
<div class="container">
    <a class="navbar-brand text-primary fw-bold fs-5" href="/">Hole Vision</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
    <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav ms-auto">
        <li class="nav-item">
        <a class="nav-link fw-semibold fs-6 <%= currentPath === '/scan' ? 'active' : '' %>" href="/scan">Scan</a>
        </li>
        <li class="nav-item">
        <a class="nav-link fw-semibold fs-6 <%= currentPath === '/tracking-history' ? 'active' : '' %>" href="/tracking-history">Riwayat</a>
        </li>
        <% if (user && user.role === 'admin') { %>
        <li class="nav-item">
            <a class="nav-link fw-semibold fs-6 <%= currentPath === '/manage-user' ? 'active' : '' %>" href="/manage-user">Daftar Pengguna</a>
        </li>
        <% } %>
        <% if (user) { %>
        <li class="nav-item">
            <a class="nav-link fw-semibold fs-6 <%= currentPath === '/profile' ? 'active' : '' %>" href="/profile">Profil</a>
        </li>
        <% } else { %>
        <li class="nav-item">
            <a class="nav-link fw-semibold fs-6 <%= currentPath === '/login' ? 'active' : '' %>" href="/login">Log In</a>
        </li>
        <% } %>
    </ul>
    </div>
</div>
</nav>